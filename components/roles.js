import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export default function Roles() {
    return (
    <SafeAreaView>
        <Text>Roles Page
    </Text>
    </SafeAreaView>
    );
}